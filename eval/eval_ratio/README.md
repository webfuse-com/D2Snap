## Space Progression Evaluation

``` console
npm run eval:space
```

``` console
npm run eval:space:_summarize -- <results-date>
```

Tracks size ratios for `rE` = `rA` = `rT` ∈ `[0, 0.1, ..., 0.9, 1]`.